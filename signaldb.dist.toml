[database]
dsn = "sqlite://.data/signaldb.db"

[storage]
default = "local"

[storage.adapters.local]
url = "file://.data/ds"

[discovery]
# Service discovery using catalog database
dsn = "sqlite://.data/signaldb.db"
heartbeat_interval = "30s"
poll_interval = "60s"
ttl = "300s"
