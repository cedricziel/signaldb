# explore-ui-pwa-installability

Make the Explore UI installable as a PWA on mobile and desktop, with a
service worker that precaches only the app shell — never live query or
telemetry data — and updates itself silently, including in a tab left open
for days.
