# query-field-discovery

Native, tenant-scoped query introspection — sources, fields, and values over the
logical namespace — answered from the schema registry and maintained statistics
rather than by scanning signal data
