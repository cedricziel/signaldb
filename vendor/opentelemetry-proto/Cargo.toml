# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# Marks this as its own workspace root so Cargo doesn't walk up looking for
# an enclosing one (it otherwise would, since this directory lives inside
# the SignalDB workspace tree). See ../README.md for why this is vendored.
[workspace]

[package]
edition = "2021"
rust-version = "1.75.0"
name = "opentelemetry-proto"
version = "0.32.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Protobuf generated files and transformations."
homepage = "https://github.com/open-telemetry/opentelemetry-rust/tree/main/opentelemetry-proto"
readme = "README.md"
keywords = [
    "opentelemetry",
    "otlp",
    "logging",
    "tracing",
    "metrics",
]
categories = [
    "development-tools::debugging",
    "development-tools::profiling",
    "asynchronous",
]
license = "Apache-2.0"
repository = "https://github.com/open-telemetry/opentelemetry-rust/tree/main/opentelemetry-proto"

[features]
default = ["full"]
full = [
    "gen-tonic",
    "trace",
    "logs",
    "metrics",
    "zpages",
    "with-serde",
    "internal-logs",
]
gen-tonic = [
    "gen-tonic-messages",
    "tonic",
    "tonic-prost",
    "tonic/channel",
]
gen-tonic-messages = ["prost"]
internal-logs = ["opentelemetry/internal-logs"]
logs = [
    "opentelemetry/logs",
    "opentelemetry_sdk/logs",
]
metrics = [
    "opentelemetry/metrics",
    "opentelemetry_sdk/metrics",
]
profiles = []
testing = ["opentelemetry/testing"]
trace = [
    "opentelemetry/trace",
    "opentelemetry_sdk/trace",
]
with-schemars = ["schemars"]
with-serde = [
    "serde",
    "const-hex",
    "base64",
]
zpages = ["trace"]

[lib]
name = "opentelemetry_proto"
path = "src/lib.rs"
doctest = false
bench = false

[dependencies.base64]
version = "0.22.1"
optional = true

[dependencies.const-hex]
version = "1.14.1"
optional = true

[dependencies.opentelemetry]
version = "0.32"
default-features = false

[dependencies.opentelemetry_sdk]
version = "0.32"
default-features = false

[dependencies.prost]
version = "0.14"
optional = true

[dependencies.schemars]
version = "1.0"
optional = true

[dependencies.serde]
version = "1.0"
features = [
    "serde_derive",
    "std",
]
optional = true
default-features = false

[dependencies.tonic]
version = "0.14.1"
features = ["codegen"]
optional = true
default-features = false

[dependencies.tonic-prost]
version = "0.14.1"
optional = true

[lints.clippy.all]
level = "warn"
priority = 1

[lints.rust]
edition_2024_expr_fragment_specifier = "allow"
if_let_rescope = "allow"
tail_expr_drop_order = "allow"

[lints.rust.rust_2024_compatibility]
level = "warn"
priority = -1
